# Core dependencies
pandas==2.3.3
plotly==6.9.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
kaleido==1.3.0

# Jupyter (needed only if running notebooks outside VS Code)
# Remove these lines if you use VS Code's built-in Jupyter support
notebook==7.5.7
ipykernel==6.31.0
nbformat==5.10.4
